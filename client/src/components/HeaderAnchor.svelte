<a href={import.meta.env.BASE_URL} class="text-2xl font-bold text-[#ffcb02] tracking-widest">Pokedle VS</a>

<style>
  a {
    --border-color: #3760a9;
    --blur-value: 2px;

    text-shadow: 
      -2px -2px var(--blur-value) var(--border-color),  
      2px -2px var(--blur-value) var(--border-color),  
      -2px 2px var(--blur-value) var(--border-color),  
      2px 2px var(--blur-value) var(--border-color);
  }
</style>
